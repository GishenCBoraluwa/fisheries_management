# ============================================================================
# ENVIRONMENT VARIABLES - .env.docker
# ============================================================================

# Database Configuration
DB_NAME=fisheries_db
DB_USER=fisheries_user
DB_PASSWORD=Admin@2001
DATABASE_URL=postgresql://fisheries_user:Admin@2001@db:5432/fisheries_db?schema=public

# Application Configuration
NODE_ENV=production
PORT=3000

# External APIs
WEATHER_API_URL=https://api.open-meteo.com/v1
PREDICTION_API_URL=http://host.docker.internal:8000/api/v1/predictions
OPTIMIZATION_API_URL=http://host.docker.internal:8000/api/v1/optimization

# CORS Configuration
CORS_ORIGIN=http://localhost,https://your-frontend-domain.com

# PgAdmin Configuration
PGADMIN_EMAIL=admin@fisheries.com
PGADMIN_PASSWORD=secure_admin_password

# Redis Configuration (if using)
REDIS_URL=redis://redis:6378
